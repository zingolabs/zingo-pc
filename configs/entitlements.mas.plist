<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- The bundled nym-proxy runs a localhost SOCKS5 listener the wallet dials;
         the inherited child uses the parent's entitlements, so the server grant
         lives here. -->
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.files.bookmarks.app-scope</key>
    <true/>
    <!-- Reading a payment QR code with the camera, in Send. Frames are decoded
         in the app and never stored or sent. -->
    <key>com.apple.security.device.camera</key>
    <true/>
  </dict>
</plist>
